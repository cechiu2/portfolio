import type { Metadata } from "next";
import { Young_Serif, Geist } from "next/font/google";
import localFont from "next/font/local";
import "./globals.css";
import CustomCursor from "@/components/CustomCursor";

// Young Serif — editorial display serif for headings. Weight 400 only (no bold variant).
const youngSerif = Young_Serif({
  variable: "--font-young-serif",
  subsets: ["latin"],
  weight: "400",
});

// Geist Sans — variable font, all weights available without explicit weight list.
const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

// Cyrene — decorative display font used exclusively for the "Claire Chiu" name mark.
const cyrene = localFont({
  src: "../public/fonts/Cyrene.otf",
  variable: "--font-cyrene",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${youngSerif.variable} ${geistSans.variable} ${cyrene.variable} antialiased`}
      >
        <a
          href="#main-content"
          className="sr-only focus:not-sr-only focus:fixed focus:left-4 focus:top-4 focus:z-50 focus:rounded-full focus:bg-purple focus:px-6 focus:py-3 focus:text-sm focus:font-semibold focus:text-white focus:shadow-lg focus:outline-none focus:ring-[3px] focus:ring-purple focus:ring-offset-2 focus:ring-offset-background"
        >
          Skip to main content
        </a>
        {children}
        <CustomCursor />
      </body>
    </html>
  );
}
